<Window x:Class="GUI.View.EditProfesorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:GUI"
        xmlns:data="clr-namespace:GUI.View"
        mc:Ignorable="d"
        Title="{DynamicResource AppTitleEProf}" Height="800" Width="800">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <local:LocalizedResourceDictionary />
            </ResourceDictionary.MergedDictionaries>
            <local:AddressConverter x:Key="AddressConverter" />
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="10">
        <TabControl>
            <TabItem Tag="Info" Header="{DynamicResource Information}">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Label Grid.Row="0" Grid.Column="0" Content="{DynamicResource FirstName}" VerticalAlignment="Center" Margin="5" />
                    <TextBox x:Name="TxtIme" Grid.Row="0" Grid.Column="1" Margin="5"
         Text="{Binding TempIme, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextChanged="ValidateInputs"/>
                    <Label x:Name="LblImeError" Grid.Row="1" Grid.Column="1" Margin="5" Foreground="Red"/>

                    <Label Grid.Row="2" Grid.Column="0" Content="{DynamicResource LastName}" VerticalAlignment="Center" Margin="5" />
                    <TextBox x:Name="TxtPrezime" Grid.Row="2" Grid.Column="1" Margin="5"
         Text="{Binding TempPrezime, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextChanged="ValidateInputs"/>
                    <Label x:Name="LblPrezimeError" Grid.Row="3" Grid.Column="1" Margin="5" Foreground="Red"/>

                    <Label Grid.Row="4" Grid.Column="0" Content="{DynamicResource BirthDate}" VerticalAlignment="Center" Margin="5" />
                    <TextBox x:Name="TxtDatumRodjenja" Grid.Row="4" Grid.Column="1" Margin="5"
         Text="{Binding TempDatumRodjenja, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat={}{0:dd/MM/yyyy}}" TextChanged="ValidateInputs"/>
                    <Label x:Name="LblDatumRodjenjaError" Grid.Row="5" Grid.Column="1" Margin="5" Foreground="Red"/>

                    <Label Grid.Row="6" Grid.Column="0" Content="{DynamicResource Address}" VerticalAlignment="Center" Margin="5" />
                    <TextBox x:Name="TxtAdresaStanovanja" Grid.Row="6" Grid.Column="1" Margin="5"
         Text="{Binding TempAdresa, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextChanged="ValidateInputs"/>
                    <Label x:Name="LblAdresaStanovanjaError" Grid.Row="7" Grid.Column="1" Margin="5" Foreground="Red"/>

                    <Label Grid.Row="8" Grid.Column="0" Content="{DynamicResource PhoneNumber}" VerticalAlignment="Center" Margin="5" />
                    <TextBox x:Name="TxtBrojTelefona" Grid.Row="8" Grid.Column="1" Margin="5"
         Text="{Binding TempTelefon, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextChanged="ValidateInputs"/>
                    <Label x:Name="LblBrojTelefonaError" Grid.Row="9" Grid.Column="1" Margin="5" Foreground="Red"/>

                    <Label Grid.Row="10" Grid.Column="0" Content="{DynamicResource Email}" VerticalAlignment="Center" Margin="5" />
                    <TextBox x:Name="TxtEmailAdresa" Grid.Row="10" Grid.Column="1" Margin="5"
         Text="{Binding TempEmail, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextChanged="ValidateInputs"/>
                    <Label x:Name="LblEmailAdresaError" Grid.Row="11" Grid.Column="1" Margin="5" Foreground="Red"/>

                    <Label Grid.Row="12" Grid.Column="0" Content="{DynamicResource Title}" VerticalAlignment="Center" Margin="5" />
                    <TextBox x:Name="TxtZvanje" Grid.Row="12" Grid.Column="1" Margin="5"
         Text="{Binding TempZvanje, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextChanged="ValidateInputs"/>
                    <Label x:Name="LblZvanjeError" Grid.Row="13" Grid.Column="1" Margin="5" Foreground="Red"/>

                    <Label Grid.Row="14" Grid.Column="0" Content="{DynamicResource IDNumber}" VerticalAlignment="Center" Margin="5" />
                    <TextBox x:Name="TxtLicnaKarta" Grid.Row="14" Grid.Column="1" Margin="5"
         Text="{Binding TempLicnaKarta, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextChanged="ValidateInputs"/>
                    <Label x:Name="LblLicnaKartaError" Grid.Row="15" Grid.Column="1" Margin="5" Foreground="Red"/>

                    <Label Grid.Row="16" Grid.Column="0" Content="{DynamicResource YearsOfWork}" VerticalAlignment="Center" Margin="5" />
                    <TextBox x:Name="TxtGodineStaza" Grid.Row="16" Grid.Column="1" Margin="5"
         Text="{Binding TempGodineStaza, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextChanged="ValidateInputs"/>
                    <Label x:Name="LblGodineStazaError" Grid.Row="17" Grid.Column="1" Margin="5" Foreground="Red"/>


                    <!-- Buttons -->
                    <StackPanel Grid.Column="0" Grid.Row="18" Grid.ColumnSpan="2" Orientation="Horizontal"
                                HorizontalAlignment="Center" Height="50" Margin="5">
                        <Button x:Name="BtnPotvrdi" Content="{DynamicResource Confirm}" Width="100" Height="45" Margin="5" Click="ConfirmButton_Click" IsEnabled="False"/>
                        <Button Content="{DynamicResource Cancel}" Width="100" Height="45" Margin="5" Click="CancelButton_Click" />
                    </StackPanel>
                </Grid>
            </TabItem>
            <TabItem Tag="Predmeti" Header="{DynamicResource Courses}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Row="0" Grid.Column="0" Content="{DynamicResource AddSubject}" Width="100" Margin="10"
                            Click="AddSubject_Click"
                            HorizontalAlignment="Left" />
                    <Button Grid.Row="0" Grid.Column="1" Content="{DynamicResource RemoveSubject}" Width="100" Margin="10"
                            Click="RemoveSubject_Click"
                            HorizontalAlignment="Left" />
                    <DataGrid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" x:Name="GridSubjects"
                              AutoGenerateColumns="False" IsReadOnly="True" Margin="0,2,0,0"
                              ItemsSource="{Binding EditProfesor.SpisakPredmeta, UpdateSourceTrigger=PropertyChanged}"
                              SelectedItem="{Binding SelectedPredmet, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding SifraPredmeta}" Width="*">
                                <DataGridTextColumn.Header>
                                    <TextBlock Text="Subject code" />
                                </DataGridTextColumn.Header>
                            </DataGridTextColumn>
                            <DataGridTextColumn Binding="{Binding NazivPredmeta}" Width="*">
                                <DataGridTextColumn.Header>
                                    <TextBlock Text="Subject name" />
                                </DataGridTextColumn.Header>
                            </DataGridTextColumn>
                            <DataGridTextColumn Binding="{Binding GodinaStudija}" Width="*">
                                <DataGridTextColumn.Header>
                                    <TextBlock Text="Year" />
                                </DataGridTextColumn.Header>
                            </DataGridTextColumn>
                            <DataGridTextColumn Binding="{Binding Semestar}" Width="*">
                                <DataGridTextColumn.Header>
                                    <TextBlock Text="Semester" />
                                </DataGridTextColumn.Header>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem Tag="Studenti" Header="Students">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="10">
                        <TextBox x:Name="TxtSearchStudent" Width="200" Margin="5"/>
                        <Button Content="Search" Width="100" Margin="5" Click="SearchStudent_Click"/>
                    </StackPanel>
                    <DataGrid x:Name="GridStudents" AutoGenerateColumns="False" IsReadOnly="True" Margin="10"
                              ItemsSource="{Binding StudentDisplays, UpdateSourceTrigger=PropertyChanged}"
                              SelectedItem="{Binding SelectedStudent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1">
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding Ime}" Width="*" Header="First Name" />
                            <DataGridTextColumn Binding="{Binding Prezime}" Width="*" Header="Last Name" />
                            <DataGridTextColumn Binding="{Binding BrojIndeksa}" Width="*" Header="Index Number" />
                            <DataGridTextColumn Binding="{Binding TrenutnaGodinaStudija}" Width="*" Header="Year" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
