<Window x:Class="GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI"
        mc:Ignorable="d"
        Title="{DynamicResource AppTitle}" Height="450" Width="800"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <local:LocalizedResourceDictionary/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Menu VerticalAlignment="Top" HorizontalAlignment="Stretch">
            <MenuItem Header="{DynamicResource File}" />
            <MenuItem Header="{DynamicResource Edit}" />
            <MenuItem Header="{DynamicResource Help}" />
        </Menu>
        <ToolBarTray Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Stretch" Margin="0,30,0,0">
            <ToolBar>
                <Button Content="{DynamicResource ToolbarButton}" />
                <ComboBox Width="100" SelectionChanged="LanguageSelectionChanged">
                    <ComboBoxItem Content="English" Tag="en"/>
                    <ComboBoxItem Content="Srpski" Tag="sr"/>
                </ComboBox>
            </ToolBar>
        </ToolBarTray>
        <TabControl Grid.Row="2" Margin="10,60,10,10">
            <TabItem Header="{DynamicResource Students}">
                <Grid>
                    <DataGrid x:Name="GridStudents" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,2,0,0" ItemsSource="{Binding Students}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding Ime}" Width="*">
                                <DataGridTextColumn.Header>
                                    <TextBlock Text="{DynamicResource FirstName}" />
                                </DataGridTextColumn.Header>
                            </DataGridTextColumn>
                            <DataGridTextColumn Binding="{Binding Prezime}" Width="*">
                                <DataGridTextColumn.Header>
                                    <TextBlock Text="{DynamicResource LastName}" />
                                </DataGridTextColumn.Header>
                            </DataGridTextColumn>
                            <DataGridTextColumn Binding="{Binding Status}" Width="*">
                                <DataGridTextColumn.Header>
                                    <TextBlock Text="{DynamicResource Status}" />
                                </DataGridTextColumn.Header>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem Header="{DynamicResource Professors}">
            </TabItem>
            <TabItem Header="{DynamicResource Courses}">
            </TabItem>
        </TabControl>
        <StatusBar VerticalAlignment="Bottom" HorizontalAlignment="Stretch">
            <StatusBarItem Content="{DynamicResource StatusText}"/>
            <StatusBarItem HorizontalAlignment="Right" Content="12:30 26.10.2020."/>
        </StatusBar>
    </Grid>
</Window>
